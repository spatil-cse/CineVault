# ===========================
# CineVault - Application Config
# ===========================
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/cinevault?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=shivvzzyy
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=cinevault_super_secret_key_change_this_in_production_2025
app.jwt.expiration-ms=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:5500,file://

# Logging
logging.level.com.cinevault=DEBUG
